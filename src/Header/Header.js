import React from 'react';
import './Header.css'

const Header = () => {
    return (
        <nav className='header'>
            <h4>This is a Navigation</h4>
        </nav>
    );
};

export default Header;